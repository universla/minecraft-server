name: Validar estructura del servidor
on: [push, pull_request]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Ejecutar pruebas
        run: |
          chmod +x tests/test_structure.sh
          ./tests/test_structure.sh
